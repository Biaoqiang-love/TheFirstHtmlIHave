# MolWeave 网站开发记录与后续计划

更新日期：2026-08-19

## 本次修改

本次将原来的 React Bits 动效演示页，改造成 PNG/PDF 分子结构识别项目的交互式前端原型。

### 已完成

1. 建立新的首页与统一视觉风格：保留 Aurora、DecryptedText、PixelTrail 动效，加入项目介绍和处理流程。
2. 建立文件入口：支持 PNG、JPG、PDF、多文件、文件夹和拖拽输入，图片可在浏览器本地预览。
3. 建立结果工作台：显示批次样本、PASS/REVIEW 状态、原图/渲染图对照、SMILES、Molfile、原因和事件。
4. 建立前端模拟管线：模拟“输入解析 → MolScribe → RDKit 渲染 → 视觉比较 → 最终输出”。
5. 保持 GitHub Pages 兼容：继续使用现有 Vite 基础路径和 GitHub Actions 部署结构。

## 主要代码位置

| 路径 | 作用 |
|---|---|
| `src/App.jsx` | 首页、工作台切换、文件状态和模拟任务调度 |
| `src/components/UploadPanel.jsx` | 拖拽、文件和文件夹选择入口 |
| `src/components/Workspace.jsx` | 批次列表、图片对照、SMILES/Molfile 与审查信息 |
| `src/mockPipeline.js` | 当前前端模拟数据与处理阶段 |
| `src/index.css` | 全站样式与移动端响应式布局 |

## 当前边界

GitHub Pages 只能托管静态前端，无法直接运行 Python、MolScribe、RDKit 或本地模型。当前版本是可交互前端原型；要处理真实 PNG/PDF，需要把 `pb_3` 管线包装成后端 API，并部署到能够运行 Python 和模型的服务器。

## 后续计划

### Phase 1：定义接口

- `POST /api/jobs`：上传文件并创建任务。
- `GET /api/jobs/{job_id}`：获取批次进度。
- `GET /api/jobs/{job_id}/events`：使用 SSE 推送逐样本事件。
- `GET /api/jobs/{job_id}/samples`：取得 SMILES、Molfile、SVG、状态和原因。

### Phase 2：封装 pb_3 后端

- PNG 直接调用 Stage A；PDF 先调用 Stage B，再把 PNG 送入 Stage A。
- 为任务建立隔离输出目录，把终端日志转换成结构化事件。
- 增加任务取消、超时、失败恢复和并发限制。

### Phase 3：接入真实前端数据

- 用真实 API 替换 `mockPipeline.js`。
- 增加上传进度、取消任务、失败重试和断线恢复。
- 使用后端返回的原图、SVG、SMILES、Molfile、PASS/REVIEW 原因。

### Phase 4：部署与安全

- GitHub Pages 继续部署前端；Python 管线单独部署后端。
- Qwen API Key 只保存在后端环境变量，绝不写入浏览器代码。
- 增加文件大小、类型、并发、超时和输出清理规则。

### Phase 5：产品完善

- 增加筛选、搜索、人工复核和整批结果导出。
- 支持下载 SMILES、Molfile、SVG、JSON。
- 展示结构修正前后差异与 Graph Patch 记录。
- 使用真实数据完成桌面端、移动端和大批量测试。
